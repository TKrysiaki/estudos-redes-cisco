## 🔧 Configuração básica de interfaces no roteador (Cisco)

### 📡 Descobrindo as interfaces

- `enable` → entra no modo privilegiado
- `show ip interface brief` → mostra as portas e se estão **UP/DOWN**

---

### ⚙️ Configurando o roteador (primeira interface)

- `conf t` → entra em modo de configuração
- `interface g0/0` → escolhe a porta
- `ip address 192.168.1.1 255.255.255.0` → define o IP
- `no shutdown` → ativa a interface (**obrigatório**)
- `exit` → volta

---

### 🔌 Se tiver outro cabo (segunda interface)

- `interface g0/1`
- `ip address 192.168.2.1 255.255.255.0`
- `no shutdown`
- `exit`

---

### ✅ Testando conexão

- `show ip interface brief` → deve aparecer **UP / UP**
- `ping 192.168.1.10` → IP do PC configurado

Se responder, a comunicação está funcionando.

---

### 💾 Salvando as configurações

Para não perder ao reiniciar o roteador:

